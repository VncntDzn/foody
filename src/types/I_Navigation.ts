import React from 'react';

export namespace I_Navigation {
  export interface I_ElevatedScroll {
    children?: any;
    window?: any;
  }
  export interface I_Navbar {
    props?: any;
  }
  export interface I_MainLayout {
    children?: React.ReactNode;
  }
}
